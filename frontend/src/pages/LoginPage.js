import {useForm} from "react-hook-form";

const LoginPage = () => {
    const {handleSubmit, register } = useForm()

    return (

        <div>
            LoginPage
        </div>
    );
};

export {LoginPage};